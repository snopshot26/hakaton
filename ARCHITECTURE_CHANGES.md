# Архитектурные изменения: Plan → Commit → Execute Pipeline

## Что изменилось

### A) TickCoordinator (интегрирован в `Bot.tick()`)

**Проблема**: Повторные запросы `/api/arena`, разные snapshot для агентов, повторный replanning

**Решение**:
- ✅ **1 запрос `/api/arena` на тик** - кэширование по `tick_count`
- ✅ **Один snapshot для всех агентов** - `state` передается всем агентам
- ✅ **Запрет повторного replanning** - `planned_actions` словарь предотвращает повторное планирование
- ✅ **Версионирование arena** - `arena_version` для отслеживания изменений

**Код**: `src/bot.py` lines 56-82

### B) RateLimiter / RequestScheduler

**Проблема**: 429 Rate Limited, нет глобального лимитера, параллельные запросы

**Решение**:
- ✅ **Глобальный RateLimiter** - единый лимитер для всех API запросов
- ✅ **RequestScheduler** - очередь для `/api/move` (1 запрос за раз, max 5 в очереди)
- ✅ **Обработка 429** - Retry-After header, exponential backoff + jitter
- ✅ **Rollback при 429** - резервы откатываются если действие не выполнено

**Код**: 
- `src/rate_limiter.py` - новый модуль
- `src/bot.py` lines 20-25, 154-185
- `src/client.py` lines 81-162

### C) ReservationManager (2-фазный резерв)

**Проблема**: Резервы ставятся до подтверждения, неатомарные, без TTL, без rollback

**Решение**:
- ✅ **SOFT reservations** - временные резервы во время планирования (очищаются каждый тик)
- ✅ **HARD reservations** - постоянные резервы после успешного API ответа (TTL=3 тика)
- ✅ **Owner tracking** - каждый резерв имеет `owner=agentId`
- ✅ **Self-reservation allowed** - агент не видит свои резервы как "занятые"
- ✅ **TTL и expiration** - старые HARD резервы автоматически истекают
- ✅ **Rollback** - при ошибке API все резервы агента откатываются

**Код**: 
- `src/reservations.py` - новый модуль
- `src/planner.py` lines 35-46, 539-548, все использования обновлены

### D) Action Execution

**Проблема**: Повторный поиск целей, нет хранения траектории

**Решение**:
- ✅ **1 action на агента на тик** - `planned_actions` предотвращает повторное планирование
- ✅ **Хранение плана** - `planned_actions[bomber_id] = (path, bomb_pos)`
- ✅ **Проверка MOVING состояния** - планирование только для READY агентов
- ✅ **Upgrade SOFT → HARD** - после успешного API ответа

**Код**: `src/bot.py` lines 89-185, `src/planner.py` lines 630-800

### E) Логирование

**Добавлено**:
- ✅ `📍 SOFT reserved` / `✅ HARD reserved` - статус резервов
- ✅ `⏸️ Already planned this tick` - предотвращение replanning
- ✅ `🔄 Rolled back reservations` - откат при ошибке
- ✅ `⏰ Expired N HARD reservations` - истечение TTL
- ✅ `📊 Planning summary` - статистика планирования
- ✅ `✅ HARD reserved after successful move` - подтверждение действия

**Код**: Все модули обновлены с детальным логированием

## Структура изменений

### Новые файлы:
1. **`src/reservations.py`** - ReservationManager с 2-фазной системой
2. **`src/rate_limiter.py`** - RateLimiter и RequestScheduler
3. **`tests/test_reservation_manager.py`** - тесты для резервов
4. **`tests/test_rate_limiter.py`** - тесты для rate limiting

### Измененные файлы:
1. **`src/bot.py`** - TickCoordinator логика, интеграция RateLimiter/RequestScheduler
2. **`src/planner.py`** - использование ReservationManager, предотвращение replanning
3. **`src/client.py`** - поддержка глобального RateLimiter, обработка 429

## Как проверить

### 1. Запустить бота и проверить логи:

```bash
python main.py
```

**Ожидаемое поведение**:
- ✅ Нет повторных "Searching for targets" для одного агента в одном тике
- ✅ Нет "reserved by another agent" сразу после собственного резерва
- ✅ "SOFT reserved" → "HARD reserved" после успешного move
- ✅ "Rolled back reservations" при 429/ошибке
- ✅ 1 запрос `/api/arena` на тик (логи показывают "Using cached" или "Fetched")
- ✅ Значительно меньше 429 ошибок

### 2. Запустить тесты:

```bash
pytest tests/test_reservation_manager.py -v
pytest tests/test_rate_limiter.py -v
```

**Ожидаемый результат**: Все тесты проходят

### 3. Проверить сценарии:

**Сценарий A: Два агента на одной клетке**
- Ожидание: Разные первые шаги, логи показывают разные резервы
- Проверка: Нет "reserved by another agent" для собственных резервов

**Сценарий B: 429 на /api/move**
- Ожидание: Резервы откатываются, план сохраняется для следующего тика
- Проверка: Логи показывают "Rolled back reservations", нет залипших резервов

**Сценарий C: Повторный replanning**
- Ожидание: "Already planned this tick, skipping"
- Проверка: Нет повторных "Searching for targets" для одного агента

## Критерии готовности

✅ **1. Нет "reserved by another agent" после собственного резерва**
   - Проверка: `is_reserved(pos, owner)` возвращает `False` для self-reservation

✅ **2. 1 arena fetch на тик, максимум 1 действие на агента**
   - Проверка: Логи показывают "Using cached" или 1 "Fetched" на тик
   - Проверка: `planned_actions` предотвращает повторное планирование

✅ **3. При 429 резервы не залипают, план переносится**
   - Проверка: `rollback_owner()` вызывается при ошибке
   - Проверка: План сохраняется в `planned_actions` для следующего тика

✅ **4. Количество 429 заметно падает**
   - Проверка: RequestScheduler очередь, RateLimiter backoff
   - Проверка: Логи показывают меньше 429 ошибок

## Детали реализации

### ReservationManager API:
```python
# SOFT reservation (во время планирования)
manager.soft_reserve(pos, owner, next_step, current_tick) -> bool

# HARD reservation (после успешного API)
manager.hard_reserve(pos, owner, next_step, current_tick, ttl=3) -> bool

# Проверка резерва (self-reservation allowed)
manager.is_reserved(pos, owner) -> bool  # False если owner == резерв owner

# Откат при ошибке
manager.rollback_owner(owner, current_tick)

# Очистка SOFT резервов (каждый тик)
manager.reset_soft_reservations()

# Истечение старых HARD резервов
manager.expire_old_reservations(current_tick)
```

### RateLimiter API:
```python
# Проверка/получение токенов
limiter.acquire() -> bool
limiter.wait_time() -> float

# Обработка 429
limiter.handle_429(retry_after=None, base_backoff=0.5)

# Сброс после успеха
limiter.reset_429()
```

### RequestScheduler API:
```python
# Постановка в очередь
scheduler.schedule_move(bombers) -> bool

# Обработка очереди
scheduler.process_queue(make_request_func) -> Optional[response]
```

## Миграция

Все изменения обратно совместимы:
- Старый код с `reserved_destinations` заменен на `ReservationManager`
- `Planner` принимает опциональный `reservation_manager` (создает по умолчанию)
- `Bot` создает глобальные компоненты, но старый код продолжит работать

## TODO / Будущие улучшения

- [ ] Хранение траектории между тиками (продолжение пути без перепланирования)
- [ ] Умное определение "arena changed" для перепланирования
- [ ] Метрики: количество 429, среднее время backoff, размер очереди

